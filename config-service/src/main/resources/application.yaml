server:
  port: 8084
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/JuanTrejo95/Microservicios
          search-paths: config-data
  application:
    name: config-service
